pipeline {
  agent any

  stages {
    stage ('init') {
      steps {
        load('env-dev.groovy')
        sh("echo Dealing with ${env.repoUrl} in ${env.env}")
        sh("echo ${env.SOMECREDSID_USR} | base64")
        sh("echo ${env.SOMECREDSID_PSW} | base64")
      }
    }
  }
}
